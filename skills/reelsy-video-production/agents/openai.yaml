interface:
  display_name: "Reelsy Video Production"
  short_description: "Generate the fewest valid clips and publish a playable Final"
  default_prompt: "Use $reelsy-video-production to generate a Reelsy AI video with the fewest valid clips and publish a playable Final."

policy:
  allow_implicit_invocation: true
