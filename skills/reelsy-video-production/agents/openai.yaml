interface:
  display_name: "Reelsy Video Production"
  short_description: "Generate the fewest valid clips for the Canvas"
  default_prompt: "Use $reelsy-video-production to generate the fewest valid Reelsy clips and show the ready results on the Canvas."

policy:
  allow_implicit_invocation: true
