interface:
  display_name: "Reelsy Song Rewrite"
  short_description: "Rewrite lyric songs from source video audio"
  default_prompt: "Use $reelsy-song-rewrite to rewrite the song in my video with approved lyrics and finish the video locally."

policy:
  allow_implicit_invocation: true
